{
  "name": "lumiverse-psyche-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun build src/backend.ts --outfile dist/backend.js --target bun && bun build src/frontend.ts --outfile dist/frontend.js --target browser",
    "test": "bun test"
  },
  "dependencies": {
    "@psyche/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "esbuild": "^0.28.0",
    "lumiverse-spindle-types": "^0.5.25",
    "typescript": "^6.0.3"
  }
}
